# ESTE ARCHIVO ES SOLO PARA REFERENCIA
# NO COMMITEAR A GIT - Usar las variables de Render dashboard

# En Render, configura estas variables en el dashboard:
# 1. FLASK_ENV=production
# 2. PRODUCTION=1
# 3. SECRET_KEY=(Generar con: python -c "import secrets; print(secrets.token_hex(32))")
# 4. DATABASE_URL=(Render lo proporciona automáticamente)

# Para generar SECRET_KEY:
# python -c "import secrets; print(secrets.token_hex(32))"

# Ejemplo de como se vería en production (NO USES ESTOS VALORES):
# FLASK_ENV=production
# PRODUCTION=1
# SECRET_KEY=abcdef123456...
# DATABASE_URL=postgresql://user:pass@host:5432/kairos
